<% icon = icon.nil? ? true : icon %>
<% text = text.nil? ? false : text %>
<% html = [] %>
<% html << content_tag(:i, nil, class: 'bi bi-trash') if icon %>
<% html << content_tag(:span, l(:button_delete)) if text %>

<%= link_to auth_source_path(source), method: :delete, class: 'btn btn-danger', title: l(:button_delete) do %>
  <%= html.compact.join('&nbsp;').html_safe %>
<% end %>