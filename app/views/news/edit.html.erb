<h2><%=l(:label_news)%></h2>

<%= labelled_form_for @news, html: { multipart: true, method: :put } do |f| %>
  <%= render 'form', f: f %>
  <%= submit_tag l(:button_save), class: 'btn btn-primary' %>
<% end %>

<% content_for :header_tags do %>
  <%= stylesheet_link_tag 'scm' %>
<% end %>
