<% items = [] %>
<% items << { content: l(:button_import), options: { url: new_users_import_path } } if User.current.allowed_to?(:import_users, nil, global: true) %>

<%= page_dropdown items, button: { content: page_icon('three-dots', title: l(:button_actions)), options: { mode: :no_icon } } if items.present? %>