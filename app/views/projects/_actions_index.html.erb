<% if User.current.allowed_to?(:add_project, nil, global: true) %>
  <%= link_to(l(:label_project_new), new_project_path, class: 'btn btn-success') %>
<% end %>

<% if User.current.admin? %>
  <%= link_to(l(:label_administration), admin_projects_path, class: 'btn btn-primary') %>
<% end %>