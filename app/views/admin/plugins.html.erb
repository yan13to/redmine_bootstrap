<% html_title l(:label_plugins) %>

<%= page_title page_heading(:h2, [page_icon(:plugins), l(:label_plugins)].join('&nbsp;').html_safe) %>

<% if @plugins.any? %>
  <%= page_table do %>
    <%= page_table_tbody render(partial: 'admin/plugin', collection: @plugins) %>
  <% end %>
  <%= page_paragraph page_link_to(l(:label_check_for_updates), '#') %>
<% else %>
  <%= page_empty %>
<% end %>

<%= javascript_tag do %>
<% end if @plugins.any? %>
