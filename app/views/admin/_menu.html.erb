<% mode ||= :flush %>

<%= page_list mode: mode, tag: :div do %>
  <% menu_items_for(:admin_menu) do |node| %>
    <% caption, url, selected = extract_node_details(node) %>
    <%= page_list_link caption, url, icon: node.name, active: selected, data: { name: node.name } %>
  <% end %>
<% end %>