<%= page_heading :h2, l(:label_issue_category_new) %>

<%= page_card do %>
  <%= labelled_form_for @category, as: :issue_category, url: project_issue_categories_path(@project) do |f| %>
    <%= render 'issue_categories/form', f: f %>
    <%= page_submit_tag l(:button_create) %>
  <% end %>
<% end %>