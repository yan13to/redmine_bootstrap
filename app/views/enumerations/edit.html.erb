<%= page_title content_tag(:h2, page_breadcrumb([{name: page_link_to(l(@enumeration.option_name), enumerations_path)}, {name: @enumeration.name, current: true}])) %>

<%= page_card do %>
  <%= render 'form', url: enumeration_path(@enumeration), method: :put, action_button: :button_save %>
<% end %>
